[project]
name = "apify-yelp-api"
version = "0.1.0"
description = "Example client for the Yelp API Apify Actors (Search, Business Details, Reviews)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
